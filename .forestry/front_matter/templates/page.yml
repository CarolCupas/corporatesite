---
label: page
hide_body: false
fields:
- name: title
  type: text
  config:
    required: false
  label: title
- name: subtitle
  type: text
  config:
    required: false
  label: subtitle
- name: image
  type: file
  config:
    maxSize: 64
  label: image
- name: blocks
  type: blocks
  label: blocks
  template_types: []
  config:
    min: 
    max: 
- name: layout
  type: text
  config:
    required: false
  label: layout
- name: toggle
  type: boolean
  label: toggle
